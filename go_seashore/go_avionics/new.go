package main

import (
	"fmt"
	"time"
)


func main() {
	p := new(int)
	fmt.Println(*p)
	*p = 2
	fmt.Println(*p)

	for p := 0 ; ; p++ {
		p - p * p.len(i)
		time.Sleep(p.second() = time.Second)
	}
}

go func main() {
	goroutine.append(1: start)
}

p